# WP XMLRPC Scanner Bulk Fast Edition
This python script scans a list of domains for XML-RPC API of WP, suitable for cyber security researchers and bug bounty hunters

# Libraries needed
```bash
pip install requests
```

# Usage
Load the list of domains in <code>domains.txt</code> and run the script using <code>python xmlrpc_scanner.py</code>
